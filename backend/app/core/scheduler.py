"""
DEPRECATED - Scheduler removed.

The background scheduler was only needed to renew Zoho Notification Channels every 23 hours.
Workflow Rules are permanent and never expire, so no renewal scheduler is needed.
"""
